#ifndef APP_TRACKING_CONFIG_H
#define APP_TRACKING_CONFIG_H

#ifdef __cplusplus
extern "C" {
#endif

/* =========================================================================
 *  系統
 * ========================================================================= */
#define SYS_BOOT_CALIBRATION_MS      5000U   /* 開機校正時間 */
#define SYS_CONTROL_PERIOD_MS        5U      /* PID 週期,可用 1/2/5 */
#define SYS_TELEMETRY_PERIOD_MS      100U    /* 遙測輸出週期 */

/* =========================================================================
 *  ADC
 * ========================================================================= */
#define ADC_12BIT_MAX                4095U
#define ADC_INVERT                   1       /* 硬體分壓反向時設 1 (亮→值低) */
#define ADC_LPF_ALPHA_NEW            9       /* 低通: new*9 + old*1,除以 10 */

/* =========================================================================
 *  LDR 追蹤判定
 * ========================================================================= */
#define LDR_CHANNEL_COUNT            4U
#define LDR_BASELINE_MARGIN          10U     /* 校正後 noise floor 額外裕度 */
#define LDR_MIN_NOISE_FLOOR          6U
#define TRACK_VALID_TOTAL_MIN        140U    /* 4 路 delta 加總最小值 */
#define TRACK_DIRECTION_CONTRAST_MIN 28U     /* 最亮-最暗差,小於不追蹤 */

/* =========================================================================
 *  控制 - 共用(純比例,沒有記憶:不積分、不微分、不速率限制)
 *  每個 cycle 的馬達速度只看當下誤差,沒有歷史 → 不會來回擺盪
 *
 *  [2026-04-18 調參] 靈敏度提高 + PID 調小(不要劇烈動)
 *    - 死區縮小:對小誤差更敏感,才抓得到微弱偏移
 *    - 三段門檻拉回靠近中心:小位移也會觸發,但配合較小 KP → 動得慢而穩
 * ========================================================================= */
#define PID_ERR_DEADBAND             0.020f  /* 由 0.050 降到 0.020,提高靈敏度 */
#define PID_ERR_SMALL                0.060f  /* 由 0.100 降到 0.060 */
#define PID_ERR_MEDIUM               0.150f  /* 由 0.250 降到 0.150 */

/* 陰影防呆:|error| 上限。
 * 當某側 LDR 完全在陰影時 delta=0,另一側爆亮,error 會飆到 ±1.0,
 * 造成馬達全速衝向「其實只是陰影邊緣」的方向。
 * 超過此值時直接飽和,避免離譜大幅度轉動。 */
#define TRACK_ERR_CAP                0.7f

/* =========================================================================
 *  追蹤方向翻轉(只影響 TRACKING 模式,manual 不受影響)
 *  機構裝反、或想整體調轉時,把對應軸改成 -1 即可
 * ========================================================================= */
#define M1_TRACK_DIR                 (+1)    /* Motor1 (水平軸): +1 正常, -1 反向 */
#define M2_TRACK_DIR                 (-1)    /* Motor2 (垂直軸): +1 正常, -1 反向 */

/* M1 過 HOME 正側時,LDR 相對 M2 的左右關係翻面,M2 輸出要反向。
 * 用 hysteresis 避免在臨界點切換抖動:
 *   axis1 > ON  才開始翻
 *   axis1 < OFF 才取消翻
 * 中間 (OFF, ON) 這段保留目前狀態不切換 */
#define M2_FLIP_WHEN_M1_POSITIVE     1       /* 1=啟用, 0=停用 */
#define M2_FLIP_M1_ON_STEPS          150     /* 超過此值開始翻 */
#define M2_FLIP_M1_OFF_STEPS         50      /* 低於此值取消翻 */

/* =========================================================================
 *  軟限位(Motor1 + Motor2)
 *  用 hz × dt 累加估算位置,超界時該方向 hz 自動 clamp 為 0。
 *  細分 1/32 → 6400 step/馬達圈
 *
 *  -- Motor1 (水平/tilt)  齒輪比 1:1 --
 *    機構 ±45° = 馬達 ±45° = ±(6400 × 45 / 360) = ±800 step
 *
 *  -- Motor2 (旋轉/azimuth)  齒輪比 1:3 --
 *    機構 ±180° = 馬達 ±540° = ±(6400 × 540 / 360) = ±9600 step
 *    (日出到日落約 180°,限 ±180° 剛好不會過頭亂繞)
 *    若需要更大範圍(例如環境需要背面追):
 *      ±270° 機構 = ±14400 step
 *      ±360° 機構 = ±19200 step
 *
 *  啟用前務必確認:
 *    1. 開機或送 HOME 指令時,機構實際在「中間位置」(位置歸零)
 *    2. manual 模式也會累加位置,若 manual 突破限位,tracking 回來會被擋住
 * ========================================================================= */
#define M1_LIMIT_ENABLE              1       /* 1=啟用, 0=停用 */
#define M1_LIMIT_STEPS               800     /* ±step (= ±45°機構) */
#define M2_LIMIT_ENABLE              1       /* 1=啟用, 0=停用 */
#define M2_LIMIT_STEPS               9600    /* ±step (= ±180°機構,1:3 齒輪比) */

/* =========================================================================
 *  控制 - Motor1 (水平軸)
 *  輸出 hz = kp × error × OUTPUT_GAIN × (pos_scale 或 neg_scale)
 *
 *  [2026-04-18 調參] PID 調小,不要劇烈動
 *    - 三段 KP 全部下修:配合小死區提高靈敏度,但動作要溫和
 *    - OUTPUT_GAIN 2.0 → 1.0:整體輸出減半,避免抖動
 * ========================================================================= */
#define M1_KP_SMALL                  100.0f  /* 由 180.0f 降到 100.0f */
#define M1_KP_MEDIUM                 400.0f  /* 由 950.0f 降到 400.0f */
#define M1_KP_LARGE                  1600.0f /* 大 error 反應加倍 (800 → 1600) */
#define M1_OUTPUT_GAIN               1.0f    /* 由 2.0f 降到 1.0f */
#define M1_POS_SCALE                 1.10f   /* 未動 */
#define M1_NEG_SCALE                 1.24f   /* 未動 */
#define M1_MAX_STEP_HZ               60000U  /* 未動 */

/* =========================================================================
 *  控制 - Motor2 (垂直軸)
 *
 *  [2026-04-18 調參] 同 M1,整體 KP 下修,OUTPUT_GAIN 砍半
 * ========================================================================= */
#define M2_KP_SMALL                  60.0f   /* 由 90.0f 降到 60.0f */
#define M2_KP_MEDIUM                 280.0f  /* 由 480.0f 降到 280.0f */
#define M2_KP_LARGE                  1120.0f /* 大 error 反應加倍 (560 → 1120) */
#define M2_OUTPUT_GAIN               1.0f    /* 由 2.0f 降到 1.0f */
#define M2_POS_SCALE                 1.02f   /* 未動 */
#define M2_NEG_SCALE                 1.16f   /* 未動 */
#define M2_MAX_STEP_HZ               60000U  /* 未動 */

/* =========================================================================
 *  串口
 * ========================================================================= */
#define SERIAL_CMD_RX_LINE_MAX       32U
#define SERIAL_CMD_QUEUE_LENGTH      4U

#ifdef __cplusplus
}
#endif

#endif /* APP_TRACKING_CONFIG_H */
